% Chương 3: Thiết kế hệ thống, triển khai và phương pháp luận

% ==========================================
% CHƯƠNG 3: THIẾT KẾ HỆ THỐNG, TRIỂN KHAI VÀ PHƯƠNG PHÁP LUẬN
% ==========================================
\chapter{Thiết kế hệ thống, triển khai và phương pháp luận}

Chương này trình bày hệ thống thực nghiệm thực tế, cách triển khai, quy trình benchmark, các biến kiểm soát để bảo đảm tính công bằng giữa hai mode datapath, và khung đánh giá các đe dọa đối với tính giá trị của nghiên cứu. Mục tiêu là mô tả đủ chi tiết để có thể tái lập, nhưng vẫn tách rõ giữa phần cơ chế nền và phần bằng chứng đo đạc.

\section{Tổng quan hệ thống}
\label{sec:system-overview}

Hệ thống benchmark được triển khai trên một cụm AWS EKS duy nhất, sử dụng 03 worker nodes loại \texttt{m5.large} trong cùng một AZ để giảm nhiễu do cross-AZ. Số lượng node được giữ cố định trong suốt quá trình đo; autoscaling tắt trong giai đoạn measurement để tránh biến thiên tài nguyên làm sai lệch so sánh.

Các namespace được tách theo chức năng:
\begin{itemize}
  \item \texttt{system-observability}: stack theo dõi (Prometheus/Grafana) và công cụ quan sát luồng.
  \item \texttt{benchmark}: workload benchmark (server/client/service) và các đối tượng phục vụ test.
\end{itemize}

\begin{table}[H]
\centering
\caption{Thông số hạ tầng thực nghiệm}
\label{tab:infrastructure-specs}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{4cm}X}
\toprule
\textbf{Thành phần} & \textbf{Cấu hình} \\
\midrule
Region & ap-southeast-1 \\
Số cluster & 1 \\
Số worker node & 3 \\
Loại instance & \texttt{m5.large} \\
Phiên bản Kubernetes & Chốt theo artifact thực tế của benchmark \\
Phiên bản Cilium & Chốt theo artifact thực tế của benchmark \\
Topology & Một AZ \\
Công cụ benchmark & Fortio \\
Giám sát metrics & Prometheus + Grafana \\
Quan sát flow & Hubble \\
\bottomrule
\end{tabularx}
\end{table}

\section{Kiến trúc triển khai}
\label{sec:deployment-topology}

Topology được tổ chức để tách rõ control/observability/workload, nhưng vẫn giữ đường truyền dữ liệu gọn cho benchmark service datapath. Trong phạm vi luận văn này, luồng chính là client Fortio gọi HTTP qua Service \texttt{ClusterIP} đến HTTP echo server; monitoring stack thu metrics hệ thống, còn Hubble cung cấp flow-level evidence.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  box/.style={
    draw,
    rounded corners=2pt,
    align=center,
    minimum height=0.9cm,
    font=\small
  },
  arrow/.style={->, thick}
]
  \node[box, minimum width=3.1cm] (fortio) at (0, 2.2) {Fortio client\\namespace benchmark};
  \node[box, minimum width=3.0cm] (svc) at (4.0, 2.2) {ClusterIP Service};
  \node[box, minimum width=3.0cm] (echo) at (8.0, 2.2) {HTTP echo pods\\worker nodes};

  \node[box, minimum width=3.2cm] (modea) at (2.0, 0.3) {Mode A\\kube-proxy / iptables};
  \node[box, minimum width=3.2cm] (modeb) at (6.0, 0.3) {Mode B\\Cilium eBPF KPR};

  \node[box, minimum width=3.0cm] (prom) at (0, -1.6) {Prometheus\\metrics scrape};
  \node[box, minimum width=3.0cm] (grafana) at (4.0, -1.6) {Grafana\\dashboards};
  \node[box, minimum width=3.0cm] (hubble) at (8.0, -1.6) {Hubble\\flow evidence};

  \draw[arrow] (fortio) -- (svc);
  \draw[arrow] (svc) -- (echo);
  \draw[arrow] (modea) -- (svc);
  \draw[arrow] (modeb) -- (svc);
  \draw[arrow] (prom) -- (grafana);
  \draw[arrow] (hubble) -- (modeb);

  \draw[rounded corners=3pt, draw=gray!60] (-1.8, -0.55) rectangle (9.8, 3.15);
  \node[font=\small\bfseries] at (4.0, 3.45) {Đường dữ liệu benchmark};
  \draw[rounded corners=3pt, draw=gray!60] (-1.8, -2.25) rectangle (9.8, -0.95);
  \node[font=\small\bfseries] at (4.0, -2.55) {Quan sát và bằng chứng hỗ trợ};
\end{tikzpicture}
\caption{EKS deployment topology dùng trong benchmark}
\label{fig:eks-deployment-topology}
\end{figure}

\subsection{Giải thích luồng chạy theo sơ đồ thiết kế}
Đối chiếu với sơ đồ kiến trúc luồng chạy của hệ thống diễn ra như sau:
\begin{itemize}
  \item \textbf{Luồng request benchmark (đường dữ liệu chính):} Fortio trong namespace benchmark phát request HTTP đến Service \texttt{ClusterIP}. Từ đây request được cân bằng đến một trong các server pod replicas đặt trên worker nodes.
  \item \textbf{Khác biệt datapath giữa hai mode:}
  \begin{itemize}
    \item \textbf{Mode A:} request đi qua cơ chế service của \texttt{kube-proxy} (iptables/IPVS) tại node trước khi đến pod đích.
    \item \textbf{Mode B:} request được xử lý bởi Cilium eBPF service datapath (service map/backend map), \texttt{kube-proxy} bị vô hiệu hóa.
  \end{itemize}
  \item \textbf{Luồng quan sát metrics:} Prometheus scrape node-exporter và các endpoint metrics liên quan Cilium, sau đó Grafana dùng dữ liệu này để hiển thị dashboard theo node/pod/cluster.
  \item \textbf{Luồng quan sát flow:} Hubble relay nhận sự kiện flow từ Cilium và cho phép \texttt{hubble observe} trích xuất verdict (FORWARDED/DROPPED/ALLOWED/DENIED), đặc biệt quan trọng cho S3 khi kiểm chứng policy enforcement.
  \item \textbf{Luồng egress hạ tầng:} traffic ra ngoài cụm (ví dụ pull image hoặc truy cập dịch vụ ngoài) đi qua NAT Gateway/Internet Gateway; đây là luồng vận hành hạ tầng, không phải datapath chính đang được so sánh trong benchmark.
\end{itemize}


\section{Định nghĩa hai mode so sánh}
\label{sec:mode-definitions}

Hai mode được định nghĩa để khác nhau chủ yếu ở service datapath, giúp phép so sánh tập trung vào câu hỏi nghiên cứu:

\textbf{Mode A (baseline):}
\begin{itemize}
  \item \texttt{kube-proxy} hoạt động.
  \item Cilium chạy ở chế độ không thay thế \texttt{kube-proxy}.
  \item Workload và monitoring stack giữ nguyên.
\end{itemize}

\textbf{Mode B (eBPF KPR):}
\begin{itemize}
  \item Cilium \texttt{kubeProxyReplacement} được bật.
  \item \texttt{kube-proxy} bị vô hiệu hóa trước khi benchmark Mode B.
  \item Hubble được bật để lấy observability evidence.
\end{itemize}

\section{Nguyên tắc bảo đảm tính công bằng giữa hai mode}
\label{sec:fairness-two-modes}

Các điều kiện để đảm bảo sự công bằng giữa hai mode được duy trì xuyên suốt:
\begin{itemize}
  \item Cùng cluster, cùng node type, cùng node count.
  \item Cùng image workload, cùng service type (\texttt{ClusterIP}).
  \item Cùng duration, cùng load levels, cùng warm-up/rest policy.
  \item Chạy tuần tự Mode A rồi đến Mode B trong cùng môi trường.
  \item Reset namespace benchmark và redeploy workload giữa hai mode.
\end{itemize}

Mục đích của tập kiểm soát này là hạn chế hiện tượng so sánh nhầm do thay đổi đồng thời quá nhiều biến.

\section{Workload và công cụ sử dụng}
\label{sec:workload-toolchain}

Thành phần benchmark:
\begin{itemize}
  \item Server: HTTP echo service.
  \item Client: Fortio.
  \item Service type: \texttt{ClusterIP}.
  \item Payload và tham số Fortio được cố định trong mỗi cặp so sánh.
  \item Policy object: \texttt{CiliumNetworkPolicy} (chỉ áp dụng cho S3).
\end{itemize}

\begin{table}[H]
\centering
\caption{Cấu hình workload}
\label{tab:workload-config}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{2.8cm} >{\raggedright\arraybackslash}p{3.2cm} >{\raggedright\arraybackslash}p{3.2cm} X}
\toprule
\textbf{Thành phần} & \textbf{Image/Loại} & \textbf{Requests/Limits} & \textbf{Ghi chú} \\
\midrule
Server & \texttt{hashicorp/http-echo} & Ghi theo artifact thực tế & Endpoint đơn giản để đo service datapath \\
Client & \texttt{fortio/fortio} & Ghi theo artifact thực tế & Tạo tải benchmark trong cluster \\
Service & \texttt{ClusterIP} & -- & Mục tiêu chính của nghiên cứu \\
Policy object & \texttt{CiliumNetworkPolicy} & -- & Chỉ dùng cho S3 (Mode B) \\
\bottomrule
\end{tabularx}
\end{table}

\section{Phân tích cơ chế, truy vết và ghi nhận dữ liệu}
\label{sec:code-tracing-logging}

\subsection{Phân tích cơ chế datapath ở mức khái niệm}
Mục tiêu của phần này là mô tả đường đi gói tin ở mức cơ chế để hỗ trợ diễn giải kết quả, không phải chứng minh trực tiếp chi phí tính toán vi mô của từng bước. Nói cách khác, code-path analysis đóng vai trò nền tảng giải thích nguyên nhân khả dĩ cho các xu hướng p95/p99, thay vì thay thế phần đo đạc định lượng.

Trong phạm vi luận văn, có ba câu hỏi cơ chế chính được dùng để soi chiếu kết quả:
\begin{itemize}
  \item \textbf{Mode A xử lý Service ra sao?} Request được ánh xạ backend qua \texttt{kube-proxy} (iptables/IPVS), sau đó chuyển đến endpoint pod.
  \item \textbf{Mode B xử lý Service ra sao?} Request đi qua datapath eBPF của Cilium (service map/backend map), loại bỏ bước xử lý của \texttt{kube-proxy}.
  \item \textbf{Hubble quan sát ở đâu?} Hubble lấy flow events từ Cilium datapath để tạo bằng chứng verdict phục vụ đối chiếu ở S3.
\end{itemize}

\subsection{Truy vết và kiểm chứng trong thực nghiệm}
Thực nghiệm sử dụng bộ lệnh kiểm chứng trước, trong và sau mỗi run để bảo đảm tính hợp lệ của dữ liệu:
\begin{itemize}
  \item \texttt{kubectl get svc,endpoints}
  \item \texttt{cilium status}
  \item \texttt{hubble status}
  \item \texttt{hubble observe}
  \item \texttt{kubectl logs}
\end{itemize}

Ý nghĩa kiểm chứng của từng lệnh trong quy trình:
\begin{itemize}
  \item \texttt{kubectl get svc,endpoints}: xác nhận Service \texttt{ClusterIP} đã ánh xạ đúng endpoint pod, tránh trường hợp benchmark đo vào service rỗng hoặc endpoint stale.
  \item \texttt{cilium status}: xác nhận Cilium agent/daemonset vận hành bình thường và mode mong muốn đã được áp dụng (đặc biệt quan trọng khi chuyển Mode A sang Mode B).
  \item \texttt{hubble status}: xác nhận pipeline thu thập flow hoạt động, tránh mất evidence ở S3 do relay hoặc observer chưa sẵn sàng.
  \item \texttt{hubble observe}: thu luồng thực tế để đối chiếu verdict và hành vi policy; dùng để kiểm chứng ``đúng chức năng'' chứ không thay thế số liệu latency/RPS.
  \item \texttt{kubectl logs}: phát hiện bất thường runtime như crash loop, timeout, OOM, hoặc warning mạng có thể làm bẩn dữ liệu run.
\end{itemize}

Quy trình kiểm chứng được áp dụng theo thứ tự:
\begin{enumerate}
  \item \textbf{Pre-run:} xác nhận cluster healthy, pod ready, endpoint mapping đúng với manifest kỳ vọng.
  \item \textbf{In-run:} theo dõi log Fortio, trạng thái pod và event bất thường (restart, OOM, network warning).
  \item \textbf{Post-run:} chụp lại trạng thái hệ thống và lưu evidence để truy vết nếu phát hiện outlier.
\end{enumerate}

Đối với S3, ngoài chỉ số hiệu năng, \texttt{hubble observe} còn được dùng để xác nhận deny/allow đúng theo policy intent, giúp tránh kết luận sai khi policy chưa thực sự có hiệu lực.

Để đảm bảo tính nhất quán, nhóm áp dụng nguyên tắc: nếu kiểm chứng mode hoặc endpoint mapping không đạt, run đó bị loại và chạy lại; nếu log cho thấy lỗi ứng dụng rõ ràng (không phải hiệu năng datapath), run được gắn cờ và không dùng cho kết luận chính.

\subsection{Các artifact và nhật ký lưu vết}
Mỗi run lưu artifacts để truy vết:
\begin{itemize}
  \item \texttt{bench.log}
  \item \texttt{metadata.json}
  \item \texttt{kubectl\_get\_all.txt}, \texttt{kubectl\_top\_nodes.txt}, \texttt{events.txt}
  \item với Mode B/S3: \texttt{cilium\_status.txt}, \texttt{hubble\_status.txt}, \texttt{hubble\_flows.jsonl}
\end{itemize}

Các artifact này được tổ chức theo thư mục mode/scenario/load/run để đảm bảo nguồn gốc dữ liệu rõ ràng (data provenance), đồng thời hỗ trợ đối chiếu ngược từ bảng tổng hợp về dữ liệu thô nếu có nghi ngờ chất lượng run.

\section{Thiết kế thực nghiệm}
\label{sec:experimental-design}

\subsection{Biến độc lập}
Biến độc lập là các yếu tố được chủ động thay đổi có kiểm soát giữa các cell benchmark:
\begin{itemize}
  \item Datapath mode: A / B.
  \item Scenario: S1 / S2 / S3.
  \item Load level: L1 / L2 / L3.
  \item Policy state: OFF / ON (chỉ áp dụng cho S3 trên Mode B).
\end{itemize}

\subsection{Biến phụ thuộc}
Biến phụ thuộc là các đại lượng đầu ra dùng để trả lời RQ:
\begin{itemize}
  \item p50 / p95 / p99.
  \item Achieved RPS.
  \item Error rate.
  \item Hubble verdict evidence (cho S3).
\end{itemize}

\subsection{Biến kiểm soát}
Biến kiểm soát là các yếu tố giữ cố định để đảm bảo khác biệt quan sát được chủ yếu đến từ datapath mode hoặc policy state:
\begin{itemize}
  \item Cluster, node type, node count.
  \item Workload image, request size, service type.
  \item Run duration, warm-up duration, rest giữa các run.
  \item Monitoring stack và quy trình reset namespace.
\end{itemize}

\begin{table}[H]
\centering
\caption{Liên kết giữa câu hỏi nghiên cứu và chỉ số đo}
\label{tab:ch3-rq-metric-traceability}
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{1.1cm} >{\raggedright\arraybackslash}p{1.4cm} >{\raggedright\arraybackslash}p{1.7cm} >{\raggedright\arraybackslash}p{2.6cm} X}
\toprule
\textbf{RQ} & \textbf{Kịch bản} & \textbf{Tải} & \textbf{Chỉ số chính} & \textbf{Bằng chứng hỗ trợ} \\
\midrule
RQ1 & S1 & L1/L2/L3 & p95, p99 & RPS, node metrics \\
RQ2 & S2 & L2/L3 & p99, error rate & Phase trend, node metrics \\
RQ3 & S3 & L2/L3 & $\Delta$ p95/p99, $\Delta$ RPS & Hubble verdicts \\
\bottomrule
\end{tabularx}
\end{table}

\section{Hiệu chuẩn tải}
\label{sec:load-calibration}

Nguyên tắc chọn load level:
\begin{itemize}
  \item L1: hệ thống ổn định, lỗi thấp.
  \item L2: bắt đầu xuất hiện tail latency.
  \item L3: gần knee-point nhưng chưa collapse.
\end{itemize}

\begin{table}[H]
\centering
\caption{Các mức tải sử dụng trong benchmark}
\label{tab:load-levels}
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{1.1cm} >{\raggedright\arraybackslash}p{3.4cm} >{\raggedright\arraybackslash}p{3.4cm} X}
\toprule
\textbf{Mức tải} & \textbf{QPS} & \textbf{Concurrency} & \textbf{Mục đích} \\
\midrule
L1 & Theo artifact calibration thực tế (tham chiếu: 100) & Theo artifact calibration thực tế (tham chiếu: 8) & Baseline ổn định \\
L2 & Theo artifact calibration thực tế (tham chiếu: 400) & Theo artifact calibration thực tế (tham chiếu: 32) & Bắt đầu thấy khác biệt \\
L3 & Theo artifact calibration thực tế (tham chiếu: 800) & Theo artifact calibration thực tế (tham chiếu: 64) & Mức tải cao làm lộ rõ tail latency \\
\bottomrule
\end{tabularx}
\end{table}

\section{Quy trình chạy benchmark}
\label{sec:run-protocol}

Quy trình chạy benchmark cho mỗi tổ hợp mode-scenario-load được chuẩn hóa để giảm sai lệch giữa các lần đo:
\begin{itemize}
  \item Warm-up: 30--60 giây.
  \item Measurement window: 180 giây (hoặc theo artifact thực tế).
  \item Repeats: 3 runs cho mỗi cell.
  \item Rest giữa các run để giảm ảnh hưởng warm cache/connection state.
  \item Reset/Cleanup nếu cần trước khi chuyển mode hoặc scenario.
\end{itemize}

Ngoài thời lượng đo chính, nhóm còn kiểm tra các điều kiện dừng sớm (ví dụ crash hoặc error rate bất thường) để tránh đưa run lỗi vào tập dữ liệu tổng hợp.

\section{Các kịch bản thực nghiệm (S1, S2, S3)}
\label{sec:scenarios}

\textbf{S1 -- Steady-state service baseline}
\begin{itemize}
  \item So sánh Mode A vs Mode B trên \texttt{ClusterIP}.
  \item Chạy tại L1/L2/L3 để quan sát xu hướng theo mức tải.
  \item Trọng tâm báo cáo: p95/p99 và độ ổn định error rate khi hệ thống ở trạng thái ổn định.
\end{itemize}

\textbf{S2 -- Stress + connection churn}
\begin{itemize}
  \item So sánh Mode A vs Mode B dưới tải động.
  \item Tập trung L2/L3 để tạo stress rõ; L1 không ưu tiên cho stress test.
  \item Điều chỉnh keep-alive/churn theo cấu hình thực tế của script.
  \item Trọng tâm báo cáo: tail latency theo phase và mức dao động giữa các run.
\end{itemize}

\textbf{S3 -- Policy overhead}
\begin{itemize}
  \item Chỉ áp dụng cho Mode B.
  \item So sánh policy OFF $\rightarrow$ policy ON.
  \item Có deny case riêng để thu Hubble evidence về policy correctness.
  \item Trọng tâm báo cáo: độ chênh p95/p99 và RPS trước/sau khi bật policy.
\end{itemize}

\section{Phương pháp thống kê và tiêu chí diễn giải kết quả}
\label{sec:statistics-plan}

Các kết quả benchmark trong luận văn không được diễn giải bằng cách chỉ nhìn biểu đồ và nhận xét trực quan. Lý do là khác biệt giữa Mode A và Mode B có thể đến từ nhiều nguồn: dao động giữa các lần chạy, nhiễu hệ thống trên môi trường cloud, tác động của burst và connection churn trong S2, cũng như kích thước mẫu nhỏ ở cấp run. Vì vậy, chương này xác định trước cách tổng hợp số liệu và tiêu chí diễn giải để Chương 4 áp dụng nhất quán khi phân tích kết quả.

\subsection{Thống kê mô tả và đơn vị báo cáo}
Đơn vị phân tích chính là kết quả ở mức từng run Fortio trong một cell benchmark cố định. Một cell được xác định bởi tổ hợp mode, kịch bản và mức tải; riêng S3 thay đổi trạng thái policy OFF/ON trong Mode B. Với mỗi cell, pipeline tổng hợp các chỉ số latency, RPS và error rate từ các lần chạy lặp lại. Các percentile được lấy từ output Fortio và chuẩn hóa về millisecond trước khi tổng hợp.

Báo cáo sử dụng các thống kê mô tả như trung bình, trung vị, độ lệch chuẩn, min, max và khoảng tin cậy 95\% khi phù hợp. Các chỉ số p50, p95, p99, RPS đạt được và error rate được giữ làm đầu ra chính vì chúng phản ánh các khía cạnh khác nhau của hiệu năng: độ trễ thông thường, độ trễ phần đuôi, khả năng duy trì tải và mức lỗi HTTP. Với latency tail, đặc biệt là p99, diễn giải ưu tiên xu hướng nhất quán qua nhiều mức tải hoặc nhiều pha hơn là một điểm cải thiện đơn lẻ.

Khoảng tin cậy 95\% được hiểu là khoảng mô tả độ bất định của trung bình mẫu trong điều kiện số lần lặp hiện có:

\[
CI_{95\%} = \bar{x} \pm t \cdot \frac{s}{\sqrt{n}}.
\]

Trong đó \(\bar{x}\) là trung bình mẫu, \(s\) là độ lệch chuẩn mẫu, \(n\) là số lần lặp và \(t\) là hệ số tới hạn theo phân phối Student t. Với \(n=3\), khoảng tin cậy thường phải được đọc thận trọng vì chỉ một vài run chậm có thể làm độ rộng khoảng thay đổi đáng kể.

\subsection{Thống kê suy luận và ý nghĩa thống kê}
Để hỗ trợ diễn giải, pipeline dùng p-value theo kiểm định kiểu Welch ở mức run-level cho các so sánh phù hợp. Cách tiếp cận này hợp lý hơn việc giả định phương sai hai nhóm bằng nhau, vì các nhóm benchmark có thể có độ biến động khác nhau, đặc biệt trong S2. Cột significant được dùng như chỉ báo hỗ trợ rằng khác biệt quan sát được có đủ mạnh trong dữ liệu hiện tại hay không.

Tuy nhiên, ý nghĩa thống kê không được xem là bằng chứng tuyệt đối. Nhiều nhóm trong benchmark chỉ có \(n=3\) run, nên p-value có độ nhạy cao với nhiễu hệ thống, outlier và độ phân tán giữa các lần chạy. Vì vậy, một kết quả significant không tự động chứng minh quan hệ nhân quả tuyệt đối, và một kết quả non-significant cũng không đồng nghĩa với việc hai mode chắc chắn không khác nhau. Trong luận văn này, p-value được dùng để củng cố hoặc làm yếu mức độ tự tin của diễn giải, không thay thế phân tích cơ chế và bối cảnh kịch bản.

\subsection{Ý nghĩa thực tế của chênh lệch}
Ngoài ý nghĩa thống kê, luận văn còn xét ý nghĩa thực tế của chênh lệch. Một khác biệt có thể được đánh dấu significant nhưng vẫn quá nhỏ để có giá trị vận hành đáng kể; ngược lại, một xu hướng chưa significant vẫn có thể đáng chú ý nếu độ lớn chênh lệch lớn, xuất hiện nhất quán trong tải cao hoặc phù hợp với cơ chế datapath.

Ý nghĩa thực tế được đọc qua chênh lệch phần trăm, chênh lệch tuyệt đối theo millisecond, mức tác động lên p99, và bối cảnh của phép đo. Với so sánh Mode B so với Mode A, delta \% được tính theo:

\[
\Delta\% = \frac{B - A}{A} \times 100\%.
\]

Với S3, khi so sánh policy ON với policy OFF trong Mode B, công thức tương ứng là:

\[
\Delta\% = \frac{\text{ON} - \text{OFF}}{\text{OFF}} \times 100\%.
\]

Hệ số biến thiên CV\% được dùng để mô tả độ ổn định tương đối giữa các run:

\[
CV\% = \frac{s}{\bar{x}} \times 100\%.
\]

Error rate được đọc như tỷ lệ request lỗi trên tổng số request:

\[
\text{Error rate} = \frac{\text{số request lỗi}}{\text{tổng số request}} \times 100\%.
\]

Với S2, bối cảnh pha như ramp-up, sustained, burst và cooldown phải được xét cùng delta p99. Với S3, chênh lệch p99 cần được đọc song song với RPS và error rate để tránh kết luận quá mức về policy overhead.

\subsection{Phân tích độ nhạy và các mở rộng tùy chọn}
Các phương pháp như Holm-Bonferroni, Hedges' g, Mann-Whitney U và bootstrap CI chỉ được xem là mở rộng tùy chọn. Chúng không được trình bày như kết quả cuối của pipeline nếu chưa được triển khai và tái sinh vào output chính thức. Nếu các mở rộng này được bổ sung sau, kết luận chính cần được đối chiếu lại với output mới thay vì chỉ thêm vào phần diễn giải hiện có.

\subsection{Nguyên tắc diễn giải kết quả}
Nguyên tắc diễn giải cuối cùng là đọc đồng thời ba lớp bằng chứng: bằng chứng thống kê, độ lớn thực tế của chênh lệch và bối cảnh kịch bản. Không nên đọc kết quả theo kiểu cơ học rằng significant là ``đúng'' còn non-significant là ``không có gì''. Chương 4 áp dụng nguyên tắc này cho từng kịch bản: S1 có thể kết luận mạnh hơn nếu xu hướng nhất quán qua các mức tải; S2 phải thận trọng hơn vì burst và connection churn làm tăng biến động; S3 chỉ kết luận trong phạm vi policy đơn giản và workload hiện tại. Các bảng và ví dụ chi tiết đối chiếu công thức với output CSV được đặt ở Phụ lục~\ref{app:statistics}.


\section{Các biện pháp kiểm soát tính công bằng}
\label{sec:fairness-controls}

Phần này chuyển hóa các rủi ro thiên lệch thực nghiệm thành biện pháp kiểm soát cụ thể, giúp người đọc đánh giá mức độ tin cậy của phép so sánh A/B.

\begin{table}[H]
\centering
\caption{Các biện pháp kiểm soát tính công bằng}
\label{tab:fairness-controls}
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{3.3cm}X}
\toprule
\textbf{Mối lo} & \textbf{Kiểm soát} \\
\midrule
Config drift & Pin version; lưu Helm values, manifests, commit hash \\
Resource noise & Fixed node count, same instance type \\
Autoscaling noise & Autoscaling off trong measurement \\
State carry-over & Reset benchmark namespace, redeploy workload \\
Request mismatch & Same payload, same Fortio parameters \\
Monitoring overhead & Giữ monitoring stack cố định \\
\bottomrule
\end{tabularx}
\end{table}

\section{Các đe dọa đối với tính giá trị của nghiên cứu}
\label{sec:threats-to-validity}

Các đe dọa đối với tính giá trị được chia thành hai nhóm: \textit{internal threats} (ảnh hưởng trực tiếp đến độ đúng trong setup hiện tại) và \textit{external threats} (giới hạn khả năng suy rộng sang ngữ cảnh khác).

\begin{table}[H]
\centering
\caption{Đe dọa nội tại}
\label{tab:internal-threats}
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{2.8cm} >{\raggedright\arraybackslash}p{2.2cm} X}
\toprule
\textbf{Đe dọa} & \textbf{Ảnh hưởng} & \textbf{Giảm thiểu} \\
\midrule
Sequential A $\rightarrow$ B & Time-of-day bias & Ghi rõ limitation; giữ khoảng cách thời gian ngắn \\
Single cluster & Carry-over state & Reset namespace; re-verify mode trước mỗi block \\
CPU/scheduling noise & Ảnh hưởng p99 & Fixed resources; lặp lại nhiều run \\
Monitoring overhead & Ảnh hưởng latency & Giữ stack quan sát cố định \\
Warm cache/connection state & Làm lệch run đầu & Warm-up + rest giữa các run \\
\bottomrule
\end{tabularx}
\end{table}

\begin{table}[H]
\centering
\caption{Đe dọa ngoại tại}
\label{tab:external-threats}
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{3.1cm}X}
\toprule
\textbf{Đe dọa} & \textbf{Hạn chế suy rộng} \\
\midrule
Same-node priority & Không đại diện đầy đủ cho cross-node full study \\
ClusterIP only & Chưa đại diện NodePort/LoadBalancer \\
HTTP only & Chưa đại diện gRPC/TCP long-lived \\
Simple policy & Chưa đại diện policy complexity scaling \\
1 cluster / 1 region & Chưa đại diện đa vùng / đa cluster \\
\bottomrule
\end{tabularx}
\end{table}
