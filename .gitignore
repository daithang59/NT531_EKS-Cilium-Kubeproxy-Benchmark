# results artifacts (keep README and template, ignore benchmark data)
# results/**
# !results/.gitkeep
# !results/README.md
# !results/metadata.template.json.txt
# !results/**/*.log   # bench.log, bench_phase*.log — benchmark evidence, not local dev logs

# local kubeconfig / credentials
kubeconfig*
*.pem
*.key

# benchmark config — NEVER commit real credentials
.env

# terraform
terraform/.terraform/
terraform/*.tfstate*
terraform/*.tfplan
terraform/**/.terraform.lock.hcl

# editor / OS
.DS_Store
.vscode/
.idea/

# logs
# *.log


#omc
.omc/